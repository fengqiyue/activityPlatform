<!DOCTYPE html>
<html>
  <head>
    <title>照片墙</title>
    <meta charset="utf-8"/>
    <meta name="viewport" content="user-scalable=no, initial-scale=1, maximum-scale=1, minimum-scale=1" />
    <link rel='stylesheet' href='/stylesheets/ionic.min.css' />
    <link rel='stylesheet' href='/stylesheets/barrage.css' />
    <script src="bower_components/jquery/dist/jquery.min.js"></script>

    <script type="text/javascript"> //点击后隐藏『上传成功』
      $(document).ready(function(){
        $("#suggest").click(function(){
          $("#suggest").hide(1500);
          });
      });
  </script>
  </head>

  <body> 
    <div class="box">
      <div class="bar bar-header bar-dark" id="header">
        <h1 class="title">照片墙</h1>
        <button class="button button-clear button-positive" onclick="location='./homepage'">返回</button>
      </div>
      <% photos.forEach(function (photo, index) { %>
          <img id="img" src="uploads/<%= photo.name %>">
      <% }) %>

      <% if(success) { %>
        <h2 id="suggest" class="button button-balanced" >投票成功，点我消失<h2>
      <% } %>

      <% if(error) { %>
        <h2 id="suggest" class="button  button-balanced" ><%= error %><h2>
      <% } %>

      <div class="bar bar-footer" id="footer" style="display:flex;justify-content:flex-end;align-items:center;">
        <form action ="/upload" method="post" enctype="multipart/form-data" class="submitphotoform">
          <a href="javascript:;" class="file">选择照片
            <input type="file" name="upload" ></input>
          </a>
          <button id="submit" class="button button-balanced button-large" >提交</button>
        </form>
      </div>
    </div>  

  </body>
</html>